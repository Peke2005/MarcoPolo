services:
  - type: web
    name: frentepartido-auth
    runtime: docker
    plan: free
    rootDir: Backend
    envVars:
      - key: AUTH_STORE
        value: postgres
      - key: PGSSLMODE
        value: require
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
