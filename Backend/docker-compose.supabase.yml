services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      AUTH_STORE: postgres
      PGSSLMODE: require
      PORT: 3001
